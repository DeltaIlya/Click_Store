import { BotApiVersion, VersionedReturnType } from '../../types';
type VersionedFeature = {
    since: BotApiVersion;
    use: () => Record<string, unknown>;
};
type VersionedWebAppLike = {
    version: string;
    isVersionAtLeast: (version: BotApiVersion) => boolean;
};
export declare function createVersionedComposable<TComposable, Version extends BotApiVersion, SuggestedVersions extends BotApiVersion>(baseVersion: Version, webApp: VersionedWebAppLike, useBase: () => Record<string, unknown>, features: VersionedFeature[]): VersionedReturnType<TComposable, Version, SuggestedVersions>;
export {};
