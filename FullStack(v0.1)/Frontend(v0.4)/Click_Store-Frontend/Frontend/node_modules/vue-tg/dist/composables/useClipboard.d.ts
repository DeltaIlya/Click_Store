import { BotApiVersion, Merge, SchemaBase, SinceLatest, SinceUntil } from '../types';
import { onClipboardTextReceived } from '../events';
type v64 = ReturnType<typeof useClipboard64>;
type AllFeatures = v64;
export type Schema = {
    '6.0': SchemaBase<AllFeatures, object>;
    '6.4': Merge<Schema['6.0'], v64>;
};
export type Clipboard = SinceUntil<Schema, '6.0', '6.4'> | SinceLatest<Schema, '6.4'>;
declare function useClipboard64(): {
    readText: {
        (): Promise<string>;
        (callback?: ((text: string) => void) | undefined): void;
    };
    onReadText: typeof onClipboardTextReceived;
};
export declare function useClipboard<Version extends BotApiVersion>(baseVersion: Version): import('../types').VersionedReturnType<Clipboard, Version, keyof Schema>;
export {};
